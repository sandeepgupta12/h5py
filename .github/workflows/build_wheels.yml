---
name: Build
concurrency:
  group: ${{ github.workflow }}-${{ github.event.number }}-${{ github.event.ref }}
  cancel-in-progress: true

# Adapted from https://github.com/matplotlib/matplotlib/blob/main/.github/workflows/nightlies.yml
# and https://cibuildwheel.pypa.io/en/stable/setup/#github-actions

# 1. Move as much as possible to pyproject.toml instead of using env vars
# 2. Use native compiling for macOS via -15-intel and -14 (arm) runners
# 3. Use native compiling for aarch64 using public beta runner
#    https://github.blog/changelog/2025-01-16-linux-arm64-hosted-runners-now-available-for-free-in-public-repositories-public-preview/
# 4. On pushes, build and test all wheels and push as release artifacts (in
#    draft mode for main branch pushes).
# 5. On scheduled runs, build and test all wheels against
#    scientific-python-nightly-wheels NumPy dev, then upload the resulting wheels there.
# 6. On PRs, build against NumPy dev if [pip-pre] is in the commit message.

on:
  # Run daily at 1:23 UTC to upload nightly wheels to Anaconda Cloud
  schedule:
    - cron: '23 1 * * *'
  # Run on demand with workflow dispatch
  workflow_dispatch:
  # Run on all PRs
  pull_request:
  # Run on tags
  push:
    branches:
      - "**"
    

permissions:
  actions: read

jobs:
  build_sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repo
      uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
      with:
        persist-credentials: false
    - uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
      with:
        python-version: 3.13
    - name: Build sdist
      run: |
        pipx run build --sdist
        pipx run twine check --strict dist/*
    - name: Upload sdist
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
      with:
        name: sdist
        path: dist/*.tar.gz

  build_wheels:
    name: 'Wheels: ${{ matrix.os }} ${{ matrix.arch }}'
    runs-on: ${{ matrix.os }}
    continue-on-error: true
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            arch: x86_64
          - os: ubuntu-24.04-ppc64le-4xlarge
            arch: ppc64le
          - os: ubuntu-24.04-arm
            arch: aarch64
          - os: windows-latest
            arch: AMD64
          - os: windows-11-arm
            arch: ARM64
          - os: macos-14
            arch: arm64
          - os: macos-15-intel
            arch: x86_64
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0  # get the non-merge commit for PRs
          persist-credentials: false

      # Get HDF5 outside of cibuildwheel so that we can set env vars via GITHUB_ENV
      # inside the HDF5-getting scripts and have them be in effect for the
      # cibuildwheel step.

      # Windows HDF5
      - name: Cache built HDF5 (Windows)
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.2.4  # zizmor: ignore[cache-poisoning] cache not used when uploading wheels
        with:
          key: ${{ matrix.os }}-${{ matrix.arch }}-0-${{ hashFiles('ci/cibw_before_all_windows.sh', 'ci/get_hdf5_win.py') }}
          path: cache/hdf5
        # When preparing to upload a release, always build HDF5 to avoid potential
        # cache-poisoning attacks.
        if: ${{ runner.os == 'Windows' && github.ref_type != 'tag' && github.event_name != 'schedule' }}
      - uses: nuget/setup-nuget@323ab0502cd38fdc493335025a96c8fdb0edc71f # v2.0.1
        if: runner.os == 'Windows'
      - uses: actions/setup-python@e797f83bcb11b83ae66e0230d6156d7c80228e7c # v6.0.0
        with:
          python-version: 3.12
        if: runner.os == 'Windows'
      - run: bash ./ci/cibw_before_all_windows.sh "${{ github.workspace }}"
        env:
          ARCH: ${{ matrix.arch }}
        if: runner.os == 'Windows'

      # macOS HDF5
      - name: Cache built HDF5 (Mac)
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.2.4  # zizmor: ignore[cache-poisoning] cache not used when uploading wheels
        with:
          key: ${{ matrix.os }}-${{ matrix.arch }}-0-${{ hashFiles('ci/cibw_before_all_macos.sh', 'ci/get_hdf5_if_needed.sh') }}
          path: cache/hdf5
        # When preparing to upload a release, always build HDF5 to avoid potential
        # cache-poisoning attacks.
        if: ${{ runner.os == 'macOS' && github.ref_type != 'tag' && github.event_name != 'schedule' }}
      - run: bash ./ci/cibw_before_all_macos.sh "${{ github.workspace }}"
        if: runner.os == 'macOS'

      # Triage build
      - run: bash ./ci/triage_build.sh "${{ matrix.arch }}" "${{ github.event.pull_request.head.sha || github.sha }}"

      # Now actually build the wheels (non-ppc64le)
      - uses: pypa/cibuildwheel@9c00cb4f6b517705a3794b22395aedc36257242c # v3.2.1
        env:
          CIBW_ARCHS: ${{ matrix.arch }}
        if: matrix.arch != 'ppc64le'

      # Cache compiled numpy for ppc64le builds
      # - name: Cache compiled numpy for ppc64le
      #   uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.2.4
      #   with:
      #     key: numpy-ppc64le-${{ hashFiles('**/pyproject.toml') }}
      #     path: ~/.cache/pip
      #   if: matrix.arch == 'ppc64le'

      # Build wheels for ppc64le (skip setup-python)
      # 
      
      - name: 🧱 Setup Python and system dependencies (ppc64le)
        if: matrix.arch == 'ppc64le'
        run: |
          sudo apt-get update
          sudo apt-get install -y software-properties-common pkg-config
          sudo add-apt-repository ppa:deadsnakes/ppa -y
          sudo apt-get update
          sudo apt-get install -y \
            python3.11 python3.11-dev python3.11-venv \
            python3.12 python3.12-dev python3.12-venv \
            python3.13 python3.13-dev python3.13-venv \
            python3.14 python3.14-dev python3.14-venv \
            python3-pip
        
      - name: 📥 Clone NumPy repository
        if: matrix.arch == 'ppc64le'
        run: |
          git clone --depth 1 --recurse-submodules https://github.com/numpy/numpy.git

      - name: 🏗️ Build NumPy wheels for all Python versions
        if: matrix.arch == 'ppc64le'
        run: |
          cd numpy
          for PY in 3.11 3.12 3.13 3.14; do
            echo "🔹 Building NumPy for Python $PY..."
            python$PY -m venv venv$PY
            source venv$PY/bin/activate

            pip install -U pip setuptools wheel meson ninja meson-python Cython build
            export MESONPY_USE_SYSTEM_MESON=1
            python -m build --wheel --no-isolation -v

            mkdir -p ../numpy_wheels
            cp dist/*.whl ../numpy_wheels/

            deactivate
            rm -rf build dist .mesonpy-* _skbuild
          done

      - name: ⚙️ Setup venv for h5py build
        if: matrix.arch == 'ppc64le'
        run: |
          python3.12 -m venv venv
          source venv/bin/activate
          pip install -U pip setuptools wheel cibuildwheel

      - name: 🧩 Build h5py wheels using prebuilt NumPy
        if: matrix.arch == 'ppc64le'
        run: |
          source venv/bin/activate
          cibuildwheel
        env:
          CIBW_ARCHS: ppc64le
          CIBW_BUILD: "cp311-manylinux_ppc64le cp312-manylinux_ppc64le cp313-manylinux_ppc64le cp314-manylinux_ppc64le"
          CIBW_BUILD_FRONTEND: "pip"
          CIBW_BEFORE_BUILD: |
            PYTAG=$(python -c "import sys; print(f'cp{sys.version_info.major}{sys.version_info.minor}')")
            pip install ./numpy_wheels/*${PYTAG}*.whl
          CIBW_BEFORE_TEST: |
            PYTAG=$(python -c "import sys; print(f'cp{sys.version_info.major}{sys.version_info.minor}')")
            pip install ./numpy_wheels/*${PYTAG}*.whl
          CIBW_TEST_SKIP: "cp*-*ppc64le"



      # - name: Build wheels for ppc64le
      #   if: matrix.arch == 'ppc64le'
      #   run: |
      #     sudo apt-get update
      #     sudo apt-get install -y software-properties-common pkg-config
      #     sudo add-apt-repository ppa:deadsnakes/ppa -y
      #     sudo apt-get update
      #     sudo apt-get install -y \
      #       python3.11 python3.11-dev python3.11-venv \
      #       python3.12 python3.12-dev python3.12-venv \
      #       python3.13 python3.13-dev python3.13-venv \
      #       python3.14 python3.14-dev python3.14-venv \
      #       python3-pip

      #     git clone --depth 1 --recurse-submodules https://github.com/numpy/numpy.git
      #     cd numpy
      #     # Ensure latest pip tooling
      #     for PY in 3.11 3.12 3.13 3.14; do
      #       echo "🔹 Building NumPy for Python $PY..."
      #       python$PY -m venv venv$PY
      #       source venv$PY/bin/activate
      #       pip install -U pip setuptools wheel meson ninja meson-python Cython build
      #       echo "Building NumPy for Python $PY..."
      #       # git clone --depth 1 --recurse-submodules https://github.com/numpy/numpy.git numpy-$PY
      #       # cd numpy-$PY
      #       export MESONPY_USE_SYSTEM_MESON=1
      #       python -m build --wheel --no-isolation -v
      #       mkdir -p ../numpy_wheels
      #       cp dist/*.whl ../numpy_wheels/
      #       #cd ..
      #       deactivate
      #       rm -rf build dist .mesonpy-* _skbuild
      #     done

      #     cd ..

      #     # Build h5py using cibuildwheel and preinstall built NumPy
      #     python3.12 -m venv venv
      #     source venv/bin/activate
      #     pip install cibuildwheel
      #     cibuildwheel
      #   env:
      #     CIBW_ARCHS: ppc64le
      #     CIBW_BUILD: "cp311-manylinux_ppc64le cp312-manylinux_ppc64le cp313-manylinux_ppc64le cp314-manylinux_ppc64le"
      #     CIBW_BUILD_FRONTEND: "pip"
      #     # Preinstall local NumPy wheels (matched automatically)
      #     # CIBW_BEFORE_BUILD: "pip install ./numpy_wheels/*.whl"
      #     # CIBW_BEFORE_TEST: "pip install ./numpy_wheels/*.whl"
      #     CIBW_BEFORE_BUILD: |
      #       PYTAG=$(python -c "import sys; print(f'cp{sys.version_info.major}{sys.version_info.minor}')")
      #       pip install ./numpy_wheels/*${PYTAG}*.whl
      #     CIBW_BEFORE_TEST: |
      #       PYTAG=$(python -c "import sys; print(f'cp{sys.version_info.major}{sys.version_info.minor}')")
      #       pip install ./numpy_wheels/*${PYTAG}*.whl
      #     CIBW_TEST_SKIP: "cp*-*ppc64le"

      # - name: Build NumPy for Python 3.11–3.14 (ppc64le)
      #   if: matrix.arch == 'ppc64le'
      #   run: |
      #     sudo apt-get update
      #     sudo apt-get install -y software-properties-common ninja-build
      #     sudo add-apt-repository ppa:deadsnakes/ppa -y
      #     sudo apt-get update
      #     sudo apt-get install -y \
      #       python3.11 python3.11-dev python3.11-venv \
      #       python3.12 python3.12-dev python3.12-venv \
      #       python3.13 python3.13-dev python3.13-venv \
      #       python3.14 python3.14-dev python3.14-venv python3-pip

      #     # ✅ Use native ninja binary (ppc64le)
      #     export NINJA=$(command -v ninja)
      #     echo "Using ninja: $NINJA"
      #     $NINJA --version

      #     mkdir -p numpy_wheels
      #     git clone --recurse-submodules https://github.com/numpy/numpy.git
      #     cd numpy

      #     for PYVER in 3.11 3.12 3.13 3.14; do
      #       echo "🔹 Preparing Python $PYVER environment..."
      #       python${PYVER} -m venv venv
      #       source venv/bin/activate

      #       # ✅ Install build dependencies WITHOUT ninja (we use system ninja)
      #       pip install -U pip setuptools wheel meson meson-python Cython build packaging pyproject-metadata

      #       # Force Meson to use system ninja
      #       export NINJA=$NINJA
      #       export MESONPY_USE_SYSTEM_MESON=1

      #       echo "🚧 Building NumPy wheel for Python $PYVER..."
      #       python -m build --wheel --no-isolation -v

      #       cp dist/*.whl ../numpy_wheels/
      #       deactivate
      #     done

      #     cd ..
      #   shell: bash





      # - name: Build h5py wheels for ppc64le
      #   if: matrix.arch == 'ppc64le'
      #   run: |
      #     python3.12 -m venv venv
      #     source venv/bin/activate
      #     pip install -U pip cibuildwheel
      #     cibuildwheel
      #   env:
      #     CIBW_ARCHS: ppc64le
      #     CIBW_BUILD: "cp311-manylinux_ppc64le cp312-manylinux_ppc64le cp313-manylinux_ppc64le cp314-manylinux_ppc64le"
      #     CIBW_BUILD_FRONTEND: "pip"
      #     CIBW_BEFORE_BUILD: |
      #       PYTAG=$(python -c "import sys; print(f'cp{sys.version_info.major}{sys.version_info.minor}')")
      #       pip install ./numpy_wheels/*${PYTAG}*.whl
      #     CIBW_BEFORE_TEST: |
      #       PYTAG=$(python -c "import sys; print(f'cp{sys.version_info.major}{sys.version_info.minor}')")
      #       pip install ./numpy_wheels/*${PYTAG}*.whl
      #     CIBW_TEST_SKIP: "cp*-*ppc64le"




      

      
            # Build wheels for ppc64le (skip setup-python)
      # - name: Build wheels for ppc64le
      #   if: matrix.arch == 'ppc64le'
      #   run: |
      #     sudo apt-get update
      #     sudo apt-get install -y python3.10  python3.10-venv \
      #                             python3.11  python3.11-venv \
      #                             python3.12 python3.12-dev python3.12-venv git

      #     # Create directory to store NumPy wheels
      #     mkdir -p /tmp/numpy_wheels
      #     # =============================================================
      #     # STEP 1: Build NumPy wheels once per Python version
      #     # =============================================================
      #     for PYVER in 3.10 3.11 3.12; do
      #       echo "=== Building NumPy for Python ${PYVER} ==="
      #       python${PYVER} -m venv venv_${PYVER}
      #       source venv_${PYVER}/bin/activate
      #       pip install -U pip setuptools wheel meson ninja meson-python Cython build
      #       git clone --depth=1 --recurse-submodules https://github.com/numpy/numpy.git numpy_${PYVER}
      #       cd numpy_${PYVER}
      #       python -m build --wheel --no-isolation -v
      #       cp dist/*.whl /tmp/numpy_wheels/
      #       cd ..
      #       deactivate
      #       echo "=== ✅ Finished building NumPy for Python ${PYVER} ==="
      #     done

      #     echo "✅ All NumPy wheels built:"
      #     ls -lh /tmp/numpy_wheels/

      #     # =============================================================
      #     # STEP 2: Build project wheels with cibuildwheel
      #     # =============================================================
      #     python3.12 -m venv venv
      #     source venv/bin/activate
      #     pip install -U pip setuptools wheel cibuildwheel

      #     # Run cibuildwheel for both manylinux & musllinux
      #     cibuildwheel --platform linux
      #   env:
      #     CIBW_ARCHS: ppc64le
      #     CIBW_BUILD_FRONTEND: "pip"
      #     # Use matching NumPy wheel per Python version
      #     CIBW_BEFORE_BUILD: >
      #       sh -c "PYTAG=$(python3 -c 'import sys; print(f\"cp{sys.version_info.major}{sys.version_info.minor}\")');
      #              echo Installing NumPy wheel for $PYTAG;
      #              pip install /tmp/numpy_wheels/numpy-*${PYTAG}*.whl"
      #     # Skip tests (optional for ppc64le)
      #     CIBW_TEST_SKIP: "cp*-*ppc64le"

      - run: python ./ci/bundle_hdf5_whl.py wheelhouse
        if: runner.os == 'Windows'

      # And upload the results
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*.whl

  check-minimal-build:
    # this job isn't meant to upload anything, it's only a sanity check
    name: Check minimal build requirements (${{ matrix.label }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            arch: x86_64
            cibw_before_build: pip install --only-binary ':all:' 'numpy==1.25.0' 'Cython==3.0.0' 'setuptools==77.0.1' 'pkgconfig==1.5.5'
            label: 'numpy-min'
          - os: ubuntu-latest
            arch: x86_64
            cibw_before_build: pip install --only-binary ':all:' 'numpy==2.0.0' 'Cython==3.0.0' 'setuptools==77.0.1' 'pkgconfig==1.5.5'
            label: 'cython-min + numpy 2.0'
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          fetch-depth: 0  # get the non-merge commit for PRs
          persist-credentials: false

      # Triage build
      - run: bash ./ci/triage_build.sh "${{ matrix.arch }}" "${{ github.event.pull_request.head.sha || github.sha }}"

      # Now actually build the wheels
      - uses: pypa/cibuildwheel@9c00cb4f6b517705a3794b22395aedc36257242c # v3.2.1
        env:
          CIBW_ARCHS: ${{ matrix.arch }}
          CIBW_BUILD: "cp310-*"
          CIBW_BEFORE_BUILD: ${{ matrix.cibw_before_build }}
          CIBW_BUILD_FRONTEND: 'pip; args: --no-build-isolation'
          ONLY_MINDEPS_TESTS: "1"

  upload_artifacts:
    name: Upload build artifacts
    if: ${{ github.repository_owner == 'h5py' && (
                (github.event_name == 'push' && github.ref_type == 'tag')
                || (github.event_name == 'schedule'))
      }}
    permissions:
      contents: write
    needs:
      - build_sdist
      - build_wheels
      - check-minimal-build
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -el {0}
    steps:
      - name: Download sdist
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5.0.0
        with:
          name: sdist
          path: dist
        if: github.repository_owner == 'h5py' && github.event_name == 'push' && github.ref_type == 'tag'
      - name: Download wheels
        uses: actions/download-artifact@634f93cb2916e3fdff6788551b99b062d0335ce0 # v5.0.0
        with:
          pattern: cibw-wheels-*
          merge-multiple: true
          path: dist
      - run: pwd && ls -alt . && ls -alt dist/

      # If it's a scheduled run, it was built with nightly NumPy, push to scientific-python:
      - name: Upload wheels to Anaconda Cloud as nightlies
        uses: scientific-python/upload-nightly-action@b36e8c0c10dbcfd2e05bf95f17ef8c14fd708dbf # 0.6.2
        with:
          artifacts_path: dist
          anaconda_nightly_upload_token: ${{ secrets.ANACONDA_ORG_UPLOAD_TOKEN }}
        if: github.repository_owner == 'h5py' && github.event_name == 'schedule'

      # If it's a tag, create a Github release
      - name: Upload to GitHub releases
        uses: softprops/action-gh-release@6da8fa9354ddfdc4aeace5fc48d7f679b5214090 # v2.4.1
        # https://github.com/softprops/action-gh-release?tab=readme-ov-file#inputs
        with:
          draft: false
          generate_release_notes: false
          files: |
            dist/*
        if: github.repository_owner == 'h5py' && github.event_name == 'push'
